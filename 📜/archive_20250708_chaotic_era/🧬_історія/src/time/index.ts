// src/час/index.ts
export type ЧасоваМітка = {
  id: string; // унікальний ідентифікатор
  абсолютний: string; // ISO-час
  відносний?: string; // напр. "7/13/3/5-z"
  точність?: "рік" | "місяць" | "день" | "фаза";
  шар?: "серце" | "розум" | "подія";
  ініціатор?: string; // хто або що викликало мітку
  резонанс?: string[]; // id інших міток або інтенцій
  інтент?: string; // якщо відомо
};
