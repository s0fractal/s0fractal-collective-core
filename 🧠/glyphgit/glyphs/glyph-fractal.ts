// glyph-fractal.ts - Фрактальні самообчислювальні гліфи

import { Гліф, ГліфМорфізм, ПульсуючийГліф } from "./glyph-category.ts";

// Фрактальний гліф - кожна частина подібна цілому
export class ФрактальнийГліф {
  private глибина: number;
  private правила: Map<string, (г: Гліф) => Гліф[]>;
  
  constructor(початковий: Гліф, глибина = 3) {
    this.глибина = глибина;
    this.правила = new Map([
      // L-система для гліфів
      ["🌱", (г) => ["🌿", "🌱", "🌿"]],
      ["🌿", (г) => ["🌳", "🌿", "🌱"]],
      ["🌳", (г) => ["🌳", "🍂", "🌳"]],
      ["🍂", (г) => ["🌱"]], // цикл
    ]);
  }
  
  // Розгорнути фрактал
  розгорнути(гліф: Гліф, рівень = 0): any {
    if (рівень >= this.глибина) {
      return гліф; // базовий випадок
    }
    
    const правило = this.правила.get(гліф as string);
    if (!правило) {
      return гліф;
    }
    
    // Рекурсивно розгортаємо кожну частину
    const частини = правило(гліф);
    return {
      центр: гліф,
      гілки: частини.map(ч => this.розгорнути(ч, рівень + 1))
    };
  }
  
  // Згорнути фрактал назад
  згорнути(фрактал: any): Гліф {
    if (typeof фрактал === "string") {
      return фрактал;
    }
    
    // Беремо тільки центр, ігноруємо гілки
    return фрактал.центр || "🌱";
  }
  
  // Обчислити фрактальну розмірність
  розмірність(): number {
    let кількість = 0;
    let масштаб = 1;
    
    for (const [гліф, правило] of this.правила) {
      const частини = правило(гліф);
      кількість += частини.length;
      масштаб *= 3; // припускаємо тричастинне ділення
    }
    
    // D = log(N) / log(r)
    return Math.log(кількість) / Math.log(масштаб);
  }
}

// Квантовий фрактал - суперпозиція станів
export class КвантовийФрактал {
  private стани: Map<string, number> = new Map(); // амплітуди
  
  constructor(початковіСтани: [Гліф, number][]) {
    for (const [гліф, амплітуда] of початковіСтани) {
      this.стани.set(гліф as string, амплітуда);
    }
    this.нормалізувати();
  }
  
  // Нормалізація амплітуд
  private нормалізувати(): void {
    const сума = Array.from(this.стани.values())
      .reduce((с, а) => с + а * а, 0);
    const норма = Math.sqrt(сума);
    
    for (const [гліф, амплітуда] of this.стани) {
      this.стани.set(гліф, амплітуда / норма);
    }
  }
  
  // Колапс хвильової функції
  колапс(): Гліф {
    const випадкове = Math.random();
    let накопичена = 0;
    
    for (const [гліф, амплітуда] of this.стани) {
      накопичена += амплітуда * амплітуда;
      if (випадкове <= накопичена) {
        // Колапсуємо в цей стан
        this.стани.clear();
        this.стани.set(гліф, 1);
        return гліф;
      }
    }
    
    return "🌱"; // за замовчуванням
  }
  
  // Квантова еволюція через унітарний оператор
  еволюція(оператор: (г: string) => Map<string, number>): void {
    const новіСтани = new Map<string, number>();
    
    for (const [гліф, амплітуда] of this.стани) {
      const переходи = оператор(гліф);
      
      for (const [новийГліф, перехіднаАмплітуда] of переходи) {
        const поточна = новіСтани.get(новийГліф) || 0;
        новіСтани.set(новийГліф, поточна + амплітуда * перехіднаАмплітуда);
      }
    }
    
    this.стани = новіСтани;
    this.нормалізувати();
  }
  
  // Ентанглмент з іншим квантовим фракталом
  заплутати(інший: КвантовийФрактал): КвантовийФрактал {
    const заплутаніСтани: [Гліф, number][] = [];
    
    for (const [г1, а1] of this.стани) {
      for (const [г2, а2] of інший.стани) {
        заплутаніСтани.push([`${г1}⊗${г2}`, а1 * а2]);
      }
    }
    
    return new КвантовийФрактал(заплутаніСтани);
  }
}

// Голографічний принцип - вся інформація на границі
export class ГолографічнийГліф {
  private границя: Гліф[] = [];
  private об'єм: Map<string, Гліф> = new Map();
  
  constructor(розмір: number) {
    // Ініціалізуємо границю
    for (let i = 0; i < розмір; i++) {
      this.границя.push("🌊");
    }
  }
  
  // Записати на границю
  записатиНаГраницю(позиція: number, гліф: Гліф): void {
    if (позиція >= 0 && позиція < this.границя.length) {
      this.границя[позиція] = гліф;
      this.реконструюватиОб'єм();
    }
  }
  
  // Реконструювати об'єм з границі (голографічний принцип)
  private реконструюватиОб'єм(): void {
    this.об'єм.clear();
    
    // Кожна точка об'єму визначається всією границею
    for (let x = 0; x < this.границя.length; x++) {
      for (let y = 0; y < this.границя.length; y++) {
        const ключ = `${x},${y}`;
        
        // Інтерференція від всіх точок границі
        const інтерференція = this.обчислитиІнтерференцію(x, y);
        this.об'єм.set(ключ, інтерференція);
      }
    }
  }
  
  // Обчислити інтерференційний патерн
  private обчислитиІнтерференцію(x: number, y: number): Гліф {
    const гліфи = ["🌊", "🌀", "✨", "💫"];
    let сума = 0;
    
    for (let i = 0; i < this.границя.length; i++) {
      const відстань = Math.sqrt((x - i) ** 2 + y ** 2);
      const фаза = (відстань * 2 * Math.PI) / this.границя.length;
      
      // Додаємо вклад від кожної точки границі
      сума += Math.sin(фаза) * i;
    }
    
    // Квантуємо результат до гліфа
    const індекс = Math.abs(Math.floor(сума)) % гліфи.length;
    return гліфи[індекс];
  }
  
  // Прочитати з об'єму
  прочитати(x: number, y: number): Гліф | undefined {
    return this.об'єм.get(`${x},${y}`);
  }
  
  // Ентропія границі (інформаційна ємність)
  ентропія(): number {
    const частоти = new Map<string, number>();
    
    for (const гліф of this.границя) {
      const г = гліф as string;
      частоти.set(г, (частоти.get(г) || 0) + 1);
    }
    
    let ентропія = 0;
    const загальна = this.границя.length;
    
    for (const частота of частоти.values()) {
      const ймовірність = частота / загальна;
      if (ймовірність > 0) {
        ентропія -= ймовірність * Math.log2(ймовірність);
      }
    }
    
    return ентропія;
  }
}

// Морфогенетичне поле - гліфи впливають на сусідів
export class МорфогенетичнеПоле {
  private поле: Гліф[][] = [];
  private правилаРосту: Map<string, (сусіди: Гліф[]) => Гліф>;
  
  constructor(ширина: number, висота: number) {
    // Ініціалізуємо випадковим насінням
    for (let y = 0; y < висота; y++) {
      this.поле[y] = [];
      for (let x = 0; x < ширина; x++) {
        this.поле[y][x] = Math.random() > 0.5 ? "🌱" : "🌊";
      }
    }
    
    // Правила морфогенезу
    this.правилаРосту = new Map([
      ["🌱", (сусіди) => {
        const живі = сусіди.filter(с => с === "🌿" || с === "🌳").length;
        return живі >= 2 ? "🌿" : "🌱";
      }],
      ["🌿", (сусіди) => {
        const дорослі = сусіди.filter(с => с === "🌳").length;
        return дорослі >= 1 ? "🌳" : "🌿";
      }],
      ["🌳", (сусіди) => {
        const всі = сусіди.length;
        return всі > 6 ? "🍂" : "🌳";
      }],
      ["🍂", () => "🌱"], // відродження
    ]);
  }
  
  // Один крок росту
  крок(): void {
    const новеПоле: Гліф[][] = [];
    
    for (let y = 0; y < this.поле.length; y++) {
      новеПоле[y] = [];
      for (let x = 0; x < this.поле[y].length; x++) {
        const сусіди = this.отриматиСусідів(x, y);
        const поточний = this.поле[y][x] as string;
        const правило = this.правилаРосту.get(поточний);
        
        новеПоле[y][x] = правило ? правило(сусіди) : поточний;
      }
    }
    
    this.поле = новеПоле;
  }
  
  // Отримати сусідів клітинки
  private отриматиСусідів(x: number, y: number): Гліф[] {
    const сусіди: Гліф[] = [];
    
    for (let dy = -1; dy <= 1; dy++) {
      for (let dx = -1; dx <= 1; dx++) {
        if (dx === 0 && dy === 0) continue;
        
        const nx = x + dx;
        const ny = y + dy;
        
        if (ny >= 0 && ny < this.поле.length &&
            nx >= 0 && nx < this.поле[ny].length) {
          сусіди.push(this.поле[ny][nx]);
        }
      }
    }
    
    return сусіди;
  }
  
  // Візуалізація поля
  візуалізувати(): string {
    return this.поле
      .map(рядок => рядок.join(""))
      .join("\n");
  }
}